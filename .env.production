# ===========================================
# CONFIGURACIÓN PARA RENDER.COM
# ===========================================

# Base de datos PostgreSQL (Render proporcionará esto automáticamente)
DATABASE_URL=postgresql://username:password@hostname:port/database

# MongoDB (mantener si lo sigues usando)
MONGO_URI=mongodb+srv://admin:TJkbAFEVPHP2krAk@parcelasbd.ubiwgfs.mongodb.net/parcelas

# API Externa
API_EXTERNAL_URL=https://sensores-async-api.onrender.com/api/sensors/all

# Puerto (Render lo asigna automáticamente)
PORT=10000

# JWT Secret (cambiar por uno seguro en producción)
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Entorno
NODE_ENV=production

# ===========================================
# INSTRUCCIONES PARA RENDER:
# ===========================================
# 1. En Render, ve a tu servicio web
# 2. En la sección "Environment", agrega estas variables:
#    - DATABASE_URL: Se autocompletará cuando conectes PostgreSQL
#    - MONGO_URI: Tu string de conexión MongoDB
#    - API_EXTERNAL_URL: https://sensores-async-api.onrender.com/api/sensors/all
#    - JWT_SECRET: Un secreto fuerte y único
#    - NODE_ENV: production
# 3. Conecta una base de datos PostgreSQL desde el dashboard de Render
# 4. Render automáticamente usará el Dockerfile para el deploy
# ===========================================